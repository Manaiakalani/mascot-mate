{
  "name": "@mascot/widget",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/mascot.js",
  "module": "./dist/mascot.js",
  "files": [
    "dist",
    "src/mascots"
  ],
  "scripts": {
    "dev": "vite",
    "visual": "node scripts/visual-check.mjs",
    "build": "vite build && tsc -p tsconfig.json --emitDeclarationOnly --outDir dist/types",
    "test": "vitest run",
    "validate-mascot": "tsx scripts/validate-mascot.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "axe-core": "^4.11.4",
    "jsdom": "^25.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
